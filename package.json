{
  "name": "opening-doors",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy",
    "deploy:prod": "twilio-run promote --production --from=dev --create-environment"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "10.17.0"
  },
  "dependencies": {
    "got": "^10.6.0"
  }
}
